The demo will include

1) read data CSV, generated tree data and stats for dataset in 
256Dimension10cluster10000Points.csv
serialize and save the index when complete

2) reload the index and perform a point query

3) reload the index and perform the wide range query

4) reload the index and perform a KNN query

5) read orignal datafile and calculate optimal theta
for a distribution with a median near the center

6) read original datafile and calculate optimal theata
for a skewed distribution with an off-center median
